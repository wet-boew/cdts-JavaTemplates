package goc.webtemplate.jsp;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * This is just a place-holder unit test class to show where to put Unit test 
 * classes in a Maven project. 
 * 
 * The src/test/java folder is independant from src/main/java and will not be part
 * of the release package jar/war.  Likewise if resource/config files need
 * to be loaded for unit testing, they would go in src/test/resources, which
 * is independant from src/main/resources.
 */
public class SampleTest
{    
    @Test
    public void testDummy_IsTrueReallyTrue()
    {
        assertTrue(true); //Yes, true should be true pretty much all the time otherwise we're in trouble
    }
}
